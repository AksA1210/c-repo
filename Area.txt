#include<iostream>
using namespace std;
class Area
{
    public:
    int area(int);
    int area(int,int);
    float area(float);
    float area(float,float); 
    int area(int,int,int);  
}
int area(int s)
{
    return(s*s);
}
int area(int l,int b)
{
    return(l*b);
}
float area(float r)
{
    return(3.14*r*r);
}
float area(float base,float h)
{
   return(0.5*base*h);
}
int area(int,int,int)
{
    int sp,k,ar;
    sp=(d+e+f)/2;
    k=sp*(sp-d)*(sp-e)*(sp-f);
    ar=k**0.5;
    return(ar)
}

int main()
{
    int s,l,b,d,e,f;;
    float r,base,h;
    cout<<"Enter side of a square:"<<" ";
    cin>>s;
    cout<<"Enter length and breadth of rectangle:"<<" ";
    cin>>l>>b;
    cout<<"Enter radius of circle:"<<" ";
    cin>>r;
    cout<<"Enter base and height of the right angled triangle:"<<" ";
    cin>>bs>>ht;
    cout<<"Enter the first side length : "<<" ";
    cin>>d;
    cout<<"Enter the second side length : "<<" ";
    cin>>e;
    cout<<"Enter the second side length : "<<" ";
    cin>>f;
    cout<<"Area of square is"<<area(s);
    cout<<"\nArea of rectangle is "<<area(l,b);
    cout<<"\nArea of circle is "<<area(r);
    cout<<"\nArea of right angled triangle is "<<area(base,h);
    cout<<"\nArea of the triangle is : "<<area(d,e,f);
}
